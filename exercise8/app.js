'use strict';

/*
Create a spec that contains 2 unit tests for Exercise 4 (Test adding a book with a ng-repeat)

1. Use npm install in the folder with package.json to install NPM dependencies (Karma, Jasmine)
2. Run ‘npm install -g karma-cli’ to install the karma client globally
3. Run ‘karma start’ to verify everything is installed.
4. Create a “Controller Test Suite” that has 2 unit tests:
	Verify the controller exists.
	Verify the addBook function works.
*/

//Write AngularJS code here!
