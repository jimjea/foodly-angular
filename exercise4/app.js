'use strict';

/*
Create an AngularJS application that displays an array of books and a button that adds a book to the array.

1. Setup to have an application with a controller
2. Add an array to the scope containing a couple of book titles.
3. Add a function to the scope that pushes a new book title to the array
4. Create a button in the index that, when clicked, invokes the add book function
5. Use the ng-repeat directive on a p element and show each book name in the index. i.e. <p ng-repeat=“apple in apples”>….</p>
*/

//Write AngularJS code here!
