'use strict';

/*
Generate an AngularJS Single Page Application

1. Install Yeoman with `npm install -g yo`
2. Run `yo` and search for a generator
3. Find and install the angular generator
4. Create a new folder, cd into the folder, and run `yo angular`
5. Install the application dependencies with `npm install` and `bower install`. Note that Yeoman tries to do this for you, but is not always successful.
6. Run `grunt serve` to start you application.

*/

//Write AngularJS code here!