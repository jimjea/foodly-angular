'use strict';

/*
Create an AngularJS application that displays a count and has a button to increment the count.

1. Use the AngularJS template to start your application.
	app.js
	Index.html
2. Create a module and attach this to the html at the body.
3. Create a controller that is a part of the module and add it as a reference in index.html with ng-controller.
4. Inject the $scope into the controller function.
5. Add a count variable and a incrementCount function.
6. Add the count and a button calling the function ng-click to index.
*/

//Write AngularJS code here!
