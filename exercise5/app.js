'use strict';

/*
Create a Multi-State Single Page Application (SPA) with two pages that display a book title and navigate towards each other.

1. Create a module and add ui.router as a dependency.
2. Create a configuration that depends on $stateProvider and add states to it.
	Use an in-line template to avoid local hosting errors, rather than templateUrl
3. Set a default URL
4. Add ui-view a div contained within the body.
*/

//Write AngularJS code here!
